#include <iostream>

using namespace std;

int main()
{
    int t;
    int a, b;

    cin >> t;
    while (t--)
    {
        cin >> a >> b;
        cout << "yes" << endl;
    }

    // 음수 가능이기에
    return 0;
}